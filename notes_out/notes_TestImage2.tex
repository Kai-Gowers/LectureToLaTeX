\documentclass{article}
\usepackage{amsmath}

\begin{document}

Let $G$ be a group. Let $H \triangleleft G$. Let $K = G/H$.

% unclear: ( Vi Ld TT Am yo (R= K/ G. let Fly 4 “Hy

We have: $|G| = |H| \cdot |K|$.

% unclear: V< le =? TOEYT (A) nG)= To Ro')-0(4') mre

Let $a \in G$, $h \in H$, $k \in K$ such that $k \in K$ and $K \triangleleft G$ so $aK = Ka$ so $K \triangleleft G$.

% unclear: | = aha, + WA he Ky tlek 50 Ken<K 80 aka K so KeG

Then: $K \triangleleft G$ so $ka = ak$ for all $k \in K$, so $(a)(k) = (k)(a)$.

% unclear: al Ee \\

$\Leftrightarrow$ $K \triangleleft G$ so $ka = ak$, $k^{-1} \in K$, so $(a)(k^{-1}) = (k^{-1})(a)$.

$k \in K$ so $aK = Ka$ and $K \triangleleft G$.

If $H \triangleleft G$ then $|K| = |G/H| = |G|/|H|$.

% unclear: a, Wat

\end{document}